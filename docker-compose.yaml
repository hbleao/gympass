version: '3'

services:
  nodejs-gympass:
    image: bitnami/postgresql
    container_name: nodejs-gympass
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: gympass
   
      
